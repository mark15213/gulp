"""Gulp shared server-side layer — the data-model contract (docs/02).

Both gulp-api and gulp-worker depend on this package so the ORM models,
db session, and settings are defined once, not duplicated. See docs/05 §4.
"""
