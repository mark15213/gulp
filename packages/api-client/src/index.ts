// The single contract surface between the Python API and the TS clients.
// `just gen-client` writes ./schema.gen.ts from the API's OpenAPI; typed
// request helpers are layered on top here. Placeholder until S0 lands the API.

export const API_CLIENT_PLACEHOLDER = true;
