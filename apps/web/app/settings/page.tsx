import React from "react";
import { SettingsHome } from "@/components/settings/SettingsHome";

export const metadata = { title: "Settings — Gulp" };

export default function SettingsPage() {
  return <SettingsHome />;
}
