// Placeholder entry. S0 Foundation fills in the mobile tab bar (docs/01 §4.3).
import { Text, View } from "react-native";

export default function Index() {
  return (
    <View>
      <Text>Gulp — mobile shell</Text>
    </View>
  );
}
